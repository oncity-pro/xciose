# 前端环境变量配置（开发环境）

# API 基础 URL（Django 后端地址）
VITE_GLOB_API_URL=http://localhost:8000/api

# 应用标题
VITE_APP_TITLE=水管家管理系统

# 是否启用 Mock（设置为 false 使用真实 API）
VITE_NITRO_MOCK=false

# 端口配置
VITE_PORT=5173

# 是否开启 HTTPS
VITE_HTTPS=false

# 是否开启压缩
VITE_COMPRESS=true

#是否开启DevTools
VITE_DEVTOOLS=true